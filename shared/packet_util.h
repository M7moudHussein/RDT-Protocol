#include <cstdint>
#include "data_packet.h"
#include "ack_packet.h"

#ifndef RDT_PROTOCOL_DATAGRAM_UTIL_H
#define RDT_PROTOCOL_DATAGRAM_UTIL_H

#endif

uint16_t calculate_checksum(data_packet) {
    return 0;
}

uint16_t calculate_checksum(ack_packet) {
    return 0;
}
